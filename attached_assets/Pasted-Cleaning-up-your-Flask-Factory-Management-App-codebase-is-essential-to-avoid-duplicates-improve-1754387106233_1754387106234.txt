Cleaning up your Flask **Factory Management App** codebase is essential to avoid duplicates, improve maintainability, and reduce UI/logic conflicts — especially since you're scaling rapidly with modules like OCR, production, accounting, batch tracking, etc.

---

## ✅ Step-by-Step Guide to Clean Your Codebase

---

### 🔹 1. **Audit the Project Structure**

Go through the **project folders**:

* Identify **duplicate files** (like `create_admin.py`, `create_approval_sample.py`, `create_batch_sample.py`) – move them into one CLI utility file or folder.
* Remove test/demo files that are no longer used.

✅ **Goal**: Keep only these at root level:

```
run.py, .env, README.md, /app, /migrations
```

---

### 🔹 2. **Refactor Models**

If you have everything in a large `models.py` file:

✅ Split it into:

* `models/core.py` → User, Supplier, Item
* `models/accounting.py` → Account, Voucher, Invoice
* `models/inventory.py` → ItemBatch, UOM
* `models/jobwork.py`, `models/bom.py`, etc.

✅ Use `__init__.py` to import them cleanly.

---

### 🔹 3. **Group Services Logically**

Move logic from views (routes) to service files:

✅ Create files like:

* `services/production_service.py`
* `services/accounting_automation.py`
* `services/batch_tracking.py`

These should contain all calculations, DB logic, and data flow — **keep routes clean and readable**.

---

### 🔹 4. **Optimize Templates and Layouts**

Check `templates/`:

✅ Structure it like this:

```
templates/
├── base.html
├── dashboard/
├── inventory/
├── accounting/
├── jobwork/
├── ocr/
```

🧹 Delete unused templates, partials, or broken layouts.

🧠 Tip: Use `includes/` for buttons, modals, and common components.

---

### 🔹 5. **Remove Duplicate Routes**

Open `/routes/`:

✅ Check for:

* Repeated route functions
* Pages using same logic but different names
* Misplaced or test routes

✅ Keep 1 route per logical operation.

🧠 If routes get too large, split them into modules like `routes/accounting_payments.py`, `routes/accounting_reports.py`.

---

### 🔹 6. **Refactor Frontend Assets**

Inside `/static/`:

✅ Remove unused CSS/JS files.

✅ Merge redundant styles into `custom.css`.

✅ Minify JS/CSS files using:

```bash
npm install -g clean-css-cli
cleancss -o custom.min.css custom.css
```

---

### 🔹 7. **Consolidate CLI Scripts**

You currently have:

```
create_admin.py
create_batch_sample.py
create_approval_sample.py
...
```

✅ Combine them into one CLI module:
`cli.py`

Example:

```python
@click.group()
def cli():
    pass

@cli.command()
def create_admin():
    ...

@cli.command()
def create_sample_batch():
    ...
```

---

### 🔹 8. **Use `.flake8` and `black` for Code Formatting**

Install:

```bash
pip install flake8 black
```

Format:

```bash
black app/ cli.py
flake8 app/
```

This cleans up indentation, spacing, and warns you of unused imports or bad practices.

---

### 🔹 9. **Run Full Cleanup & Testing**

Before deployment:

* ✅ `python -m unittest discover`
* ✅ `python cli.py check-db-tables`
* ✅ Manual UI testing (to ensure layout is not broken)
* ✅ Remove `*.pyc`, `__pycache__/`, and junk files.

---

### 🔹 10. **Create a Clean Git Commit**

```bash
git status
git add .
git commit -m "🧹 Cleanup: Removed duplicates, optimized structure and UI"
```

---

## ✅ Want Me to Auto-Generate the Refactored Structure?

I can output the **clean folder structure with boilerplate**, and migration guide step by step — just confirm:

* Do you want it in-place?
* Or as a new cloned directory like `/app_clean/`?

Let me know and I’ll help you cleanly migrate.
